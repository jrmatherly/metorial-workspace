# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### ⚙️ Miscellaneous

- Update mise.toml prepare sources to only trigger bun install on bun.lock changes.

- Validate and improve Drift configuration

- Update project ID in config.json and add Drift-specific ignore patterns to .gitignore.

- *(drift)* Update gitignore for analysis cache directories

### 🎨 Styling

- Fix markdown formatting in CLAUDE.md

- Add a blank line for improved readability before the memory list.

### 🐛 Bug Fixes

- Remove invalid schema URL from Drift config

- *(ci)* Pin mise version to fix "vlatest" URL bug

- *(ci)* Fix bash arithmetic causing false failures in validate task

- *(ci)* Exclude catalog Docker builds from default build task

- *(ci)* Add golangci-lint for engine linting

- *(ci)* Relax golangci-lint and remove monorepo config_roots warnings

- *(drift)* Correct mise tasks for multi-project workspace execution

- *(mise)* Add file-based drift tasks and fix gitignore pattern

- *(ci)* Add timeout and optimize Drift scan in GitHub Actions

### 📚 Documentation

- Update Go linting documentation to specify golangci-lint v2 and its configuration changes.

- *(serena)* Update mise memory with file-based drift task structure

- *(serena)* Update drift memory with file-based task architecture

- *(serena)* Update development workflow with distributed Drift scan architecture

### 🔄 Refactor

- Expand `.gitignore` with comprehensive rules and update `.serena/project.yml` to disable `.gitignore` parsing while adding detailed ignore paths.

- Migrate Drift tasks from `mise.toml` to file-based definitions, update `.gitignore` to track them, and add `driftdetect-dashboard` dependency.

### 🚀 Features

- Add JSON schemas for mise configuration, settings, tasks, and plugins.

- Add `bun-index` preparation step and specify full paths for `bun` dependency installations.

- Integrate Serena MCP for semantic code understanding

- Integrate Drift Detect for codebase intelligence and AI context

- Implement AGENTS.md open standard with progressive disclosure

- *(tooling)* Integrate git-cliff for automated changelog generation

- *(ci)* Add mise bootstrap script and Drift quality gate

- Configure multi-project Drift analysis with dedicated mise tasks and generate initial analysis reports.

- *(drift)* Add approve-all task with 95% confidence threshold

- *(drift)* Enhance integration with security boundaries, hooks, and CI improvements

- Add initial discovered and approved patterns along with their backups.

- *(skills)* Install 17 Agent Skills for AI-assisted development

- *(drift)* Add approve-all task with 95% confidence threshold

- Add `mise` tasks for opening the Drift dashboard for various projects.

- *(cortex)* Initialize Drift Cortex V2 memory system with mise tasks

- *(ci)* Add weekly Drift scan workflow and document architecture

<!-- Generated by git-cliff - https://git-cliff.org -->
