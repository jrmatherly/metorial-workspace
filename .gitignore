# =============================================================================
# Metorial Workspace .gitignore
# =============================================================================
# This workspace repo tracks only configuration files.
# Project repositories are cloned separately as git submodules/separate repos.
# =============================================================================

# =============================================================================
# PROJECT DIRECTORIES (separate git repos - clone independently)
# =============================================================================
metorial/
metorial-docs/
metorial-index/
metorial-platform/

# =============================================================================
# OPERATING SYSTEM FILES
# =============================================================================

# macOS
.DS_Store
.DS_Store?
._*
.AppleDouble
.LSOverride
.Spotlight-V100
.Trashes
.fseventsd
.TemporaryItems
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
*.stackdump
[Dd]esktop.ini
$RECYCLE.BIN/
*.lnk

# Linux
*~
.directory
.Trash-*
.nfs*

# =============================================================================
# EDITOR & IDE FILES
# =============================================================================

# VS Code
*.code-workspace.local
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/*.code-snippets
.history/

# JetBrains IDEs (IntelliJ, WebStorm, etc.)
.idea/
*.iml
*.ipr
*.iws
out/
.idea_modules/

# Vim/Neovim
*.swp
*.swo
*.swn
*~
.netrwhist
tags
Session.vim
.vim/

# Emacs
*~
\#*\#
/.emacs.desktop
/.emacs.desktop.lock
*.elc
auto-save-list
tramp
.\#*

# Sublime Text
*.sublime-workspace
*.sublime-project

# Atom
.atom/

# Zed
.zed/

# =============================================================================
# TOOL RUNTIME DATA
# =============================================================================

# Mise (tool version manager)
# Localized mise data directory (created by bootstrap script)
.mise/
# Exception: track mise tasks (file-based task definitions)
!.mise/tasks/
!.mise/tasks/drift/
# Note: bin/mise IS committed for CI bootstrap
# Note: mise.lock IS committed for reproducible builds
# Note: mise.toml IS committed for tool definitions

# Mise local configuration (may contain secrets/overrides)
mise.local.toml

# Serena MCP runtime data
.serena/cache/

# Claude Code local settings
.claude/settings.local.json

# =============================================================================
# ENVIRONMENT & SECRETS
# =============================================================================

# Environment files
.env
.env.local
.env.*.local
.env.development
.env.test
.env.production
.env.staging

# Private keys and certificates
*.pem
*.key
*.crt
*.p12
*.pfx
*.jks

# AWS credentials
.aws/credentials
.aws/config

# GCP service accounts
*-service-account.json
*-credentials.json
service-account*.json

# SSH keys (if accidentally placed here)
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub

# =============================================================================
# LOGS & DEBUG
# =============================================================================
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.pnpm-debug.log*

# =============================================================================
# BUILD & RUNTIME ARTIFACTS
# =============================================================================

# Dependencies (should not exist at workspace level, but just in case)
node_modules/
vendor/

# Build outputs
dist/
build/
out/
.next/
.nuxt/
.turbo/

# Cache
.cache/
cache/
.parcel-cache/

# Coverage
coverage/
*.lcov
.nyc_output/

# =============================================================================
# WORKSPACE-SPECIFIC LOCAL FILES
# =============================================================================

# Scratchpad for notes/experiments
.scratchpad/

# Local documentation cache
.local_docs/

# Local schema files
schema/

# =============================================================================
# BACKUP & TEMPORARY FILES
# =============================================================================
*.bak
*.backup
*.tmp
*.temp
*.orig
*.rej
*.patch

# Archives (typically downloaded/generated)
*.zip
*.tar
*.tar.gz
*.tgz
*.rar
*.7z

# =============================================================================
# DATABASE & DATA FILES
# =============================================================================
*.sqlite
*.sqlite3
*.db
*.db-journal
*.db-shm
*.db-wal

# =============================================================================
# MISC
# =============================================================================

# Crash reports
crash.log
hs_err_pid*

# npm/yarn package files (created during development)
*.tgz

# Diagnostic reports
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Drift: ignore caches and temporary/generated data
# Analysis caches (regenerated on demand)
.drift/lake/
.drift/cache/
.drift/history/
.drift/call-graph/
.drift/module-coupling/
.drift/test-topology/

# Discovered patterns (tracked in .drift/patterns/approved/)
.drift/patterns/discovered/
.drift/patterns/ignored/
.drift/patterns/variants/

# Constraints
.drift/constraints/discovered/
.drift/constraints/ignored/

# Contracts
.drift/contracts/discovered/
.drift/contracts/ignored/
.drift/contracts/mismatch/

# Backup files
.drift/**/.backups/

# Note: These Drift files ARE committed (project configuration):
# - .drift/config.json (project settings)
# - .drift/manifest.json (project metadata)
# - .drift/boundaries/rules.json (security rules)
# - .drift/quality-gates/policies/*.json (quality gate policies)
# - .drift/patterns/approved/*.json (approved patterns)
# - .drift/indexes/*.json (pattern indexes)
# - .drift/views/*.json (view caches for status)
